# CURRENT — what's in flight
_Claude Code: read this at session start, update it before session end._

## Now
- **`main` is merged and current — 35 agents live in production as of 2026-09-09.** `batch1-expansion` (Items A, B, C, D, E, H, Switchboard Phase 1) is now identical to `main` (both at the same commit); the "holding for go-ahead" merge from prior sessions has happened. Batches 1–3 (Rahab, Zacchaeus, Silas, Obadiah, Bezalel, Priscilla, Amos, Tabitha) are shipped and live. Gabriel remains paused — never added to the roster, not an inactive row to flip. See `docs/specs/00_STATE_OF_THE_BUILD.md` §2/§5 for the full current roster table.
- **Structural prompt-block fix, 2026-09-09.** New `app/app/prompt_composer.py` is now the single source of truth for the PROOF OF WORK and CONVERSATION DISCIPLINE blocks — every caller composes through `compose_system_prompt()` at call time instead of trusting whatever got baked into `system_prompt` text or staged in `config` by the original two sweeps. Rule: every active agent gets CONVERSATION DISCIPLINE; every agent except Eden also gets PROOF OF WORK. Defensively strips any already-baked block text from a stored `system_prompt` first, so the 8 agents whose text already had these blocks appended don't get them twice. `switchboard/composer.py` and `ops_audit.py`'s L5 smoke call both now go through it — one composer, not two. Unit test (`app/tests/test_prompt_composer.py`) composes all 35 live agents and asserts block presence/absence + no duplication; passes. A real live "hi" to Rahab with a seeded open item returned a genuine greeting plus the restated choices — CONVERSATION DISCIPLINE confirmed working end-to-end, not just unit-tested. This also confirmed the Anthropic account credit exhaustion flagged at the end of the last session is resolved — real LLM calls succeed again.
- **Data fixes, 2026-09-09** (migration `20260909120000_caleb_ops_delegation_and_finance_labels.sql`): `solomon-ceo.outside_scope` and `isaiah-cso.outside_scope` now delegate day-to-day ops / tactical execution to NEHEMIAH instead of CALEB (Caleb is CISO now, not COO — this text was stale since Phase 0.5). `fin` (Hannah) and `joanna-finance`'s `department_label` both fixed to "Finance" (were "People & Culture" and "Operations"). Scanned every agent's `outside_scope`/`helps`/`handoff_to`/`covers_for`/`covered_by`/`supervises`/`reports_to` for any other CALEB reference in an ops sense first — none found; the remaining live CALEB references (Solomon's/Nehemiah's org-chart `handoff_to`/`supervises`, Ezra's `reports_to`, Nehemiah's security-routing `outside_scope`) are all correct as-is and untouched.
- **Docs sweep, 2026-09-09**: every `/agents` and `/public/agents` route mention across `RUNBOOK.md`, `CHECKLIST.md`, and `docs/specs/*.md` replaced with `/employees/`, the real (and only) live roster route — except two deliberately-left historical passages (`00_STATE_OF_THE_BUILD.md` §5, `BATCH1_COMPLETION_REPORT.md`) that specifically narrate the now-removed `/agents` route's history, where a blind replace would have made those sentences false. No `PUNCHLIST_SWITCHBOARD_INTO_AN.md` file exists in this repo (checked, including AN-repo) — if John meant a specific file, it wasn't found under that name; flagging rather than guessing at a replacement.
- Item I (Switchboard) is at Phase 1 of 5 (widget core, backend, AN embed) — see `docs/specs/SWITCHBOARD_PHASE1_COMPLETION.md`. Phases 2–5 (proactive DMs, voice call button, other embeds, upsell checkout) not started.
- Item F (Autopilot Nuances) still queued, needs Blast Video/Higgsfield access not available in this environment.
- Item J (Specialization Engine) can start once Switchboard is far enough along to be worth specializing.

## Blockers on John
- Confirm Gabriel's status: still paused / not being built, or is this the session to start it? (`RUNBOOK.md` "H+ — Gabriel reactivation" is still queued.)
- Real Switchboard workspace onboarding is blocked: the only auto-provisioning path (exact email match to `client_profiles.contact_email`) doesn't fire for any of the 5 real active clients — none have `contact_email` set. Either backfill that field or build the real invite flow (Addendum C.3) before any real client can self-provision.
- Set Render env `SWITCHBOARD_JWT_SECRET` if not already set — the Switchboard router fails closed (503) without it.
- Google Maps API key (Distance Matrix + Geocoding) — Silas's route ordering works today via a PostGIS straight-line stand-in, but real drive-time needs this.
- A weather data source for Silas's weather-conflict job, and confirmation of Exterior Rescue's GPS clock-in feed endpoint.
- Provision a real Google Cloud OAuth client (`GOOGLE_OAUTH_CLIENT_ID`/`SECRET`) — nothing in Connections Hub can go live without this.
- A Higgsfield credential/checker for Bezalel's L3.
- AN-repo's `connections-hub` branch (Connections Hub page + Switchboard embed) is committed but still not pushed — pending your go-ahead, same hold pattern as before.

## Next up
- Item I Phases 2–5 (proactive DM pipeline from the action library, `agent_jobs`-driven presence, handoff windows, VAPI voice call button, embeds beyond AN, locked-agent checkout flow).
- Item F (Autopilot Nuances) once Blast Video/Higgsfield access is available.
- Item J (Specialization Engine), once Switchboard is further along.
- The `config->>'proof_of_work_block'` / `config->>'conversation_discipline_block'` values and the block text already baked into 8 agents' `system_prompt` are now dead data (nothing reads them — `prompt_composer.py` owns this structurally). Left in place deliberately, not migrated out; harmless.

## Last verified facts
See `docs/specs/00_STATE_OF_THE_BUILD.md` (rewritten 2026-09-09 — 35 agents, current department table, Gabriel status, `/agents` route history closed out), `docs/specs/BATCH1_COMPLETION_REPORT.md`, `docs/specs/BATCH2_3_COMPLETION_REPORT.md`, `docs/specs/CONNECTIONS_HUB_COMPLETION_REPORT.md`, `docs/specs/PROOF_OF_WORK_COMPLETION.md`, `docs/specs/SWITCHBOARD_PHASE1_COMPLETION.md`, `docs/state/OPS_AUDIT.md`. `GET /employees/?platform=automation-nation` returns 35, live-verified 2026-09-09. `/employees/` (never `/agents`, which no longer exists in the codebase) is the real roster route.
