# DECISIONS — append-only
- 2026-07-06: Fable tier for Solomon only (Option A); Option B (full C-suite on Fable) staged as a single env-var flip `MODEL_COMPLEX`.
- 2026-07-06: Eight new resellable agents specced; build in three batches (Rahab/Zacchaeus/Silas → Obadiah/Bezalel/Priscilla → Amos/Tabitha).
- 2026-07-08: Finance & Invoicing agent renamed Joanna (biblical_name); product_name stays Lydia. Shopify agent keeps Lydia. (Found already applied in DB on 2026-09-01.)
- 2026-07-08: Connections Hub rides GHL's approved social OAuth apps in v1; no custom Meta/TikTok apps.
- 2026-07-08: Token encryption = app-level Fernet, key in Render env only. Not pgcrypto.
- 2026-08-22: Unlazy adopted as dev standard, per-project on Foundation first. Proof-of-Work receipt standard adopted for all runtime agents; Eden exempt from receipt logging.
- 2026-09-01: Voice retention — transcripts kept indefinitely; raw audio hard-deleted at 30 days, client-adjustable downward only.
- 2026-09-01: Live two-way PTT approved; app ships as customer-facing software (one Foundation app, tenant at login); pricing $99 base + $12/seat + $19/channel on top of $149 platform.
- 2026-09-01: Product name = **Switchboard**. AIM rejected on trademark risk with full knowledge of the mark's status. Nostalgia goes in the marketing, not the trademark.
- 2026-09-01: The 26-agent catalog is `foundation.ai_employees` in project rhtwtoinmiekttvunlzs. `public.ai_employees` in the AN database is a homepage display table and is out of scope.
- 2026-09-02: Fable 5.1 (`claude-fable-5-1`) replaces Fable 5 in all specs. Sonnet 5 (`claude-sonnet-5`) becomes the `standard` tier.
- 2026-09-02: Caleb becomes Chief Information Security Officer (C-suite, complex tier). Nehemiah added as COO. Ezra stays Tech & IT and reports to Caleb. Security page = caleb.automaitionnation.com. Open: whether the AN homepage "Ask Caleb" button moves to Nehemiah.
- 2026-09-04: Conversation discipline is a hard rule for every agent — greetings/acks never resolve an open item; agents restate open questions; humans are never answered for. Tests block the build. See 05_CONVERSATION_DISCIPLINE.md.
- 2026-09-05: The Aug 18 "migration drift" (20260818193841/193842/195000) and item A's CORS/API-key/Fernet code were both work from an earlier session that used a different local clone (`Claude Foundation`, never pushed until today). Not mysterious, not duplicate work — reconciled onto `batch1-expansion` (pushed to origin) in this session. `/agents` returning 500 is not a schema-exposure bug (verified: both anon and service-role keys read `foundation.*` fine); it's a separate, unrelated, low-priority route (`foundation.agents`, one scaffold row) with a likely-stale Render env var, not the roster endpoint. `/employees/` is and was the real roster route, already 200. See 00_STATE §5/§9.
- 2026-09-05: Phase 0.5 (Caleb → CISO, Nehemiah → COO) shipped. Delilah (`ori`) and Leah (`leah-exec-asst`) — not named in the 0.5 spec's report-line list — were moved from Caleb to Nehemiah on the same "COO-line reports follow the new COO" logic applied to Joseph and Martha. Flagged for John to confirm, not blocking.
- 2026-09-05: Added `legacy_slug='caleb-coo'` to Caleb's row per spec (his id embeds the old "coo" role and wasn't renamed, so the legacy_slug is set to itself — purely for discoverability by anything scanning for role-changed agents).
- 2026-09-05: `docs/state/LIVE_SCHEMA_2026-09.md` and `MIGRATION_DRIFT.md` written from a real `information_schema`/`pg_catalog` capture (`supabase db query --linked`, no Docker), not inference. The RUNBOOK's guess about what the three Aug-18 migrations created (industries/industry_playbooks/llm_cost_* tables in `public`) was wrong on every point — corrected in MIGRATION_DRIFT.md. Found separately: `public.industries`, `industry_playbooks`, `llm_usage`, `pricing_config/outcomes/quotes`, `transfer_logs` — six tables with zero CLI migration history, all pre-existing, mostly empty. Not fixed (nothing broken), just documented. One actionable note: `public.llm_usage` is a second, dead, unused table distinct from the real `foundation.llm_usage` the router writes to.
- 2026-09-05: Batch 1 (item B) shipped in full — Rahab, Zacchaeus, Silas, the shared typed action library, HyperSchedule Phase 1's schema and `claim_slot()`. Roster now 30. See `docs/specs/BATCH1_COMPLETION_REPORT.md`.
- 2026-09-05: `GHL_API_KEY` found already set in this build environment mid-Batch-1 (not configured by any session) — John confirmed it's real and intentional, use it going forward. Its exact scope (which client/location it authenticates) wasn't independently verified beyond that.
- 2026-09-05: Silas's route ordering uses a real PostGIS straight-line (haversine) nearest-neighbor algorithm as a working stand-in for Google Maps Distance Matrix, since `GOOGLE_MAPS_API_KEY` isn't set. Not a stub — genuinely assigns crews and orders stops — just not drive-time-aware yet. Swap point is a single function (`_distance()` in `app/app/silas/dispatch.py`).
- 2026-09-05: Zacchaeus's Stripe backfill used clearly-labeled synthetic seed data (`scripts/zacchaeus_backfill_seed.py`) instead of a real Stripe sync, since Connections Hub (item C) hasn't shipped a working `connection_broker` OAuth flow yet. Real sync replaces it once item C lands.
- 2026-09-05: Ops Audit Part A shipped and run for real: 0 green / 1 amber / 29 red. Not treated as a bug — 27 of 30 agents genuinely have no system_prompt, and the new agents' external capabilities genuinely aren't connected yet. The system-prompt gap is explicitly NOT bundled into this pass as a quick fix — writing 27 real prompts is its own dedicated work item, flagged in OPS_AUDIT.md rather than rushed.
- 2026-09-05: `foundation.client_connections` (connection_broker's supporting table, designed back at item A) had only ever been written against the pre-CLI-tracked migration convention and was never actually applied. Applied for real via the current CLI-tracked flow while building the ops audit's L3 Stripe check, which depends on it.
- 2026-09-05: Item C (Connections Hub) built across both Foundation and AN-repo, per John's explicit call to expand scope into AN-repo/Blast Video this session. Kept the client_connections table's existing app-level Fernet encryption rather than the Connections Hub spec's own "Vault/pgsodium" line — that spec (July 6) predates and is superseded by the later, explicit Fernet decision (July 8). Investigated Blast Video's `signInWithOAuth` first and found it's a bare Supabase Auth sign-in with no scopes/callback route — not reusable, built Google OAuth from scratch instead.
- 2026-09-05: E, F, H, I, J all arrived in rapid succession alongside C. Per John's call: finish C and H this pass, hold E/F/I/J for explicit restart rather than rushing through the rest of the build order back-to-back. I is a full second product (Switchboard, XL) that J explicitly depends on — arrival order didn't match buildable order.
- 2026-09-05: Batch 2+3 (Obadiah, Bezalel, Priscilla, Amos, Tabitha) shipped with full schema/RLS/prompts, but only Amos got a standalone cron/module built this pass — a deliberate scope trim given the size of B, D, and C already done in the same session, not a silent gap. The "PROOF OF WORK block in all five prompts" verify line from item H's spec wasn't satisfiable since item E (which defines that block) is explicitly held.
- 2026-09-05: John said continue with E, F, I, J. Sequencing E → I → J → F (dependency order: J needs I; F needs Blast Video/Higgsfield access, tackled last).
- 2026-09-05: Item E (Proof of Work + Unlazy) shipped in full, both parts. Evidence derivation for §Part 2 item 7 was centralized in `executor._finalize()` rather than spread across each `connection_broker` action method as the spec literally described — same guarantee (simulated actions can never claim verified), one enforcement point instead of per-handler discipline. Item 9 (Switchboard receipt cards) deferred since Switchboard doesn't exist yet — the data model is ready for it. See `docs/specs/PROOF_OF_WORK_COMPLETION.md`.
- 2026-09-05: A `Switchboard/switchboard-foundation-build/` directory (BUILD_PACKAGE.md, CHECKLIST.md, RUNBOOK.md, demo/, docs/, assets/) appeared untracked in the Foundation repo, not authored this session — appears to be reference material John dropped in for item I. Left untouched, not bundled into item E's commit; will read fully before starting item I.
- 2026-09-05: Cleaned up incidental untracked cruft found alongside item E's files before committing: an empty `schema_snapshot.sql`, a stray file literally named `upabase migration list` (contents were a `git log` dump — clearly a mistyped redirect from an earlier command, not real output), and `.claude/settings.local.json.unlazy.bak` (the unlazy installer's automatic backup of `settings.local.json`, already itself tracked). All three deleted, none committed. `skills-lock.json` was legitimate but missed from the original Unlazy install commit (7ab62e8) — committed separately.
- 2026-09-05: Mid-move of the `Switchboard/switchboard-foundation-build/` reference package into place (per its own `BUILD_PACKAGE.md`), a shell chain error caused `rm -rf Switchboard` to run even though the preceding `git mv` commands had failed (no `set -e` — a failed command doesn't stop the rest of a multi-line bash call). This deleted `demo/switchboard-demo-v11.html`, both logo assets, and `RUNBOOK.md`/`CHECKLIST.md`/`BUILD_PACKAGE.md` before they were relocated. Recovered in full: found `switchboard-foundation-build.zip` still on the Desktop (the original unzip source), verified every extracted file's byte size against the zip's own directory listing, copied into place. Nothing was actually lost. Flagged to John directly when it happened, not glossed over. Diffed the package's bundled `docs/specs/`/`docs/state/` against the live tree first — identical except `00_STATE_OF_THE_BUILD.md` (correctly differs, state has moved on) — so that copy was NOT re-applied, only `demo/`, `assets/`, and the three root files were committed.
- 2026-09-05: While starting item I, found `05_CONVERSATION_DISCIPLINE.md` §5 explicitly requires its prompt block to ship "in item E's sweep (same sweep as PROOF OF WORK)" — item E's actual sweep only did PROOF OF WORK. Corrected with a new migration rather than silently letting the gap stand, since discovering it before Phase 1's `open_items` schema work made this the right moment. Unlike PROOF OF WORK, Eden IS included in this sweep — the rule is about not dropping a human's open question, unrelated to her receipt-logging privacy exemption. Verified live: all 35 agents covered.
- 2026-09-05: Item I (Switchboard) Phase 1 shipped. Two live-schema-driven deviations from `SWITCHBOARD_BUILD.md`'s literal SQL: (1) FKs target `foundation.client_profiles`, not the spec's nonexistent bare `clients` table; (2) added `client_agent_entitlements` (not in the spec's table list) because no client-level per-agent entitlement source exists anywhere — `employee_platform_subscriptions` is platform-level only. RLS kept the same `service_role_all_X` pattern used everywhere else in this codebase rather than the spec's literal `auth.jwt()`-based policies, since nothing here has browsers holding real Supabase-issued JWTs against this project — workspace/user isolation is enforced in the FastAPI layer via a Foundation-minted Switchboard JWT instead, verified for real (cross-workspace and within-workspace-Eden isolation both confirmed against live data). Auth bridge to AN verifies tokens via AN's own `/auth/v1/user` endpoint rather than needing AN's JWT secret. Workspace auto-bootstrap only fires on an exact `contact_email` match — doesn't work yet for any real client since none have that field set, flagged rather than worked around. Full writeup: `docs/specs/SWITCHBOARD_PHASE1_COMPLETION.md`.
- 2026-09-05: Discovered during Switchboard verification that this deployment's Anthropic account is out of API credit (`Your credit balance is too low`) — a real infrastructure/billing gap affecting every live agent, surfaced directly to John rather than treated as a Switchboard-specific issue.
- 2026-09-05: Unlazy installed per-project (not global, `-g` flag skipped) into Foundation only, per the spec's own "OPEN QUESTION FOR JOHN" and its recommended default (per-project for two weeks, then decide) — not silently using the literal `-g` the task list wrote, since the spec itself flagged that as undecided. Installed via `npx skills add Leonxlnx/unlazy` (not a git clone, so no commit SHA exists to pin) — pinned version is `2.1.0` per its package.json instead. Snyk flagged it "Med Risk" in the install-time security assessment (Gen: Safe, Socket: 0 alerts) — noted, not independently audited further. v2 already ships wave-based rolling dispatch (confirmed in references/dispatch.md: every launch in a wave opens together before the first wait) — the spec's own §1.5 parallel-dispatch edit was skipped per its explicit "if v2 already ships rolling dispatch... skip it entirely" instruction.
